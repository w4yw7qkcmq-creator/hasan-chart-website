import { buildPrivateMetadata } from "../../../lib/seo";

export const metadata = buildPrivateMetadata({ title: "برنامج الشركاء" });

export default function AffiliateLayout({ children }) {
  return children;
}
