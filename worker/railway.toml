# HasaN CharT World — worker services (Railway Root Directory = worker)
# Per-service start commands are set on Railway (Subscription Worker → subscription-maintenance-worker.js).

[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
