# Railway Worker — Root Directory MUST be `worker` for this service.
# Installs only worker/package.json dependencies (express, ws, etc.).

[variables]
NODE_VERSION = "22"

[phases.setup]
aptPkgs = ["fontconfig", "fonts-noto-core", "fonts-noto-ui-core", "fonts-noto-color-emoji"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["node -e \"require('express'); require('ws'); console.log('worker deps ok')\""]

[start]
cmd = "npm start"
