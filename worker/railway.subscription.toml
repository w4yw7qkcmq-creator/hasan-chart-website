# Subscription maintenance worker — Railway service: "Subscription Worker"
# Root Directory = worker

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node subscription-maintenance-worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
