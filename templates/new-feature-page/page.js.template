"use client";

import { UiPageShell, UiPageHeader, UiCard, UiButton } from "../../components/ui";

export default function NewFeaturePageTemplate() {
  return (
    <UiPageShell className="p-4 md:p-6">
      <UiPageHeader
        title="{{ARABIC_TITLE}}"
        subtitle="{{ENGLISH_TITLE}}"
        actions={<UiButton variant="primary">إجراء</UiButton>}
      />
      <UiCard>
        <p className="ui-text-muted text-sm">TODO: page content</p>
      </UiCard>
    </UiPageShell>
  );
}
