#!/usr/bin/env node
console.log("news worker retention policy: published_news/news_posts retained for SEO; operational dedupe uses link+fingerprint; no unbounded worker log table in DB.");
console.log("news worker retention PASS");
