# Runbook: Queue Backlog (AR-007)

## Steps
1. Check news worker and price-alerts worker on Railway
2. Verify Redis/Upstash connectivity
3. Inspect job queue depth in worker logs
4. Scale worker if sustained backlog
